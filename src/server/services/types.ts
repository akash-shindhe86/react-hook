export interface AppVersionType {
    header: string;
    footer: string;
    'on-load-event': string;
}

export interface apiOptionsType {
    cache?: Object;
    next?: Object;
    headers?: Object;
}

export interface AEMType {
    banner: string;
    config?: any;
}
